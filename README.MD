# Multiplayer Socket Ping-Pong

Basic multiplayer ping pong game using sockets and linear interpolation

## Run locally

To run locally with Docker run the following command:

```bash
  docker compose up
```
